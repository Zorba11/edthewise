# application-admin-routing-web

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test application-admin-routing-web` to execute the unit tests via [Jest](https://jestjs.io).
