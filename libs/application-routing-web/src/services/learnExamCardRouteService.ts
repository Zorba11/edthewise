import { RouterState, RouterStore } from "mobx-state-router";

export const onEnterLearnExamCard = (fromState: RouterState, toState: RouterState, routerStore: RouterStore) => {

  return Promise.resolve();
}
