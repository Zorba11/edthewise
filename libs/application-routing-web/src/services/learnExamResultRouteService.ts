import { RouterState, RouterStore } from "mobx-state-router";

export const onEnterLearnExamResult = (fromState: RouterState, toState: RouterState, routerStore: RouterStore) => {

  return Promise.resolve();
}
