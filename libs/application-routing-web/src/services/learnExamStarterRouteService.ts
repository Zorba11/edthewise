import { RouterState, RouterStore } from "mobx-state-router";

export const onEnterLearnExamStarter = (fromState: RouterState, toState: RouterState, routerStore: RouterStore) => {

  return Promise.resolve();
}
