import { commonBaseInversify } from "./common-base-inversify";

describe("commonBaseInversify", () => {
  it("should work", () => {
    expect(commonBaseInversify()).toEqual("common-base-inversify");
  });
});
