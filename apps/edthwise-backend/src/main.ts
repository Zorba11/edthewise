import express from "express";

const app = express();
const port = 3001; // You can choose a port that suits your setup

app.listen(port, () => {
  console.log(`Server is listening on port ${port}`);
});

app.listen(port, () => {
  console.log(`Server is listening on port ${port}`);
});
